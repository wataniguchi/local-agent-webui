# Copy this file to .env and set a real random key before running
# `docker compose up`. This key authenticates open-webui's requests to
# open-terminal — treat it like a password.
#
# Generate one with:
#   openssl rand -base64 32
#
OPEN_TERMINAL_API_KEY=changeme-generate-a-real-key
